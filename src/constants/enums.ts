export enum Role {
  ADMIN = 'ADMIN',
}

export enum ProductCategory {
  CLAY = 'CLAY',
  TEXTILES = 'TEXTILES',
  WOOD = 'WOOD',
}

export enum OrderStatus {
  PENDING = 'PENDING',
  PROCESSING = 'PROCESSING',
  COMPLETED = 'COMPLETED',
  CANCELLED = 'CANCELLED',
}
